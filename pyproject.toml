[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "torchlinops"
version = "0.0.1"
authors = [
    { name = "Mark Nishimura", email = "markn1@stanford.edu" }
]

[tool.ruff]
lint.ignore = ["F403"] # from ___ import *

[tool.ruff.format]
quote-style = "single"
